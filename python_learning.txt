def album_name( album_name, band_name):
    full_details = f"{album_name} {band_name}"
    return full_details.title()

while True:
    print("\nGive me album details:")
    print("(enter q at any to quit)")
    
    a_name = input ("Album name:")
    if a_name.lower() == "q":
        break

    b_name = input ("band name")
    if b_name.lower() == "q":
        break
    full_album_name = album_name(a_name, b_name)
    print(f"\n The album is {album_name}") 